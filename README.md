NEW ADDED FEATURES:
-Each book has managecopies button 
-can add multiple book copies (source and condition)
-each book copy has pull out button(pull out reason and pull out date)

-Each book has archive button
-archived books are moved to a separate archives section, where they can be restored using a restore button
